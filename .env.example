# node identity & networking
NODE_ID=node-1
RPC_HOST=0.0.0.0
RPC_PORT=8101
PEERS=http://node-2:8102/node-2,http://node-3:8103/node-3

# Redis
REDIS_URL=redis://redis:6379/0

# Raft
RAFT_ELECTION_MIN_MS=800
RAFT_ELECTION_MAX_MS=1400
RAFT_HEARTBEAT_MS=250

# Queue
QUEUE_NUM_VNODES=64
QUEUE_VISIBILITY_TIMEOUT_MS=15000

# Cache
CACHE_MAX_ITEMS=500
CACHE_BACKING_PREFIX=kv:
CACHE_DIR_PREFIX=cache:dir:
CACHE_PUBSUB_PREFIX=cache:inval:

# Failure detector
FD_HEARTBEAT_MS=1000
FD_SUSPECT_AFTER_MISSES=5

# Metrics (Prometheus)
METRICS_PORT=9100